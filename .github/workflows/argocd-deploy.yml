# Installs Argo CD on the target cluster and applies the app-of-apps entrypoint
# (bootstrap/root-app.yaml). Workloads and image tags live in this same repo under apps/.
name: Argo CD bootstrap

on:
  push:
    branches: ["main"]
  workflow_dispatch:

concurrency:
  group: argocd-bootstrap-${{ github.ref }}
  cancel-in-progress: false

permissions:
  id-token: write
  contents: read

env:
  ARGOCD_VERSION: v2.14.3
  ACK_CLUSTER_ID: ${{ vars.ACK_CLUSTER_ID || 'c6158028f3dfc4d6c8e00daf7a9c4f3e6' }}

defaults:
  run:
    shell: bash

jobs:
  deploy_argocd:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure Alibaba Cloud credentials (OIDC)
        uses: aliyun/configure-aliyun-credentials-action@v1
        with:
          role-to-assume: ${{ secrets.ROLETOASSUME }}
          oidc-provider-arn: ${{ secrets.OIDC_PROVIDER }}
          audience: ${{ secrets.AUDIENCE }}

      - name: Set K8s context
        uses: aliyun/ack-set-context@v1
        with:
          access-key-id: ${{ env.ALIBABA_CLOUD_ACCESS_KEY_ID }}
          access-key-secret: ${{ env.ALIBABA_CLOUD_ACCESS_KEY_SECRET }}
          security-token: ${{ env.ALIBABA_CLOUD_SECURITY_TOKEN }}
          cluster-id: ${{ env.ACK_CLUSTER_ID }}

      - name: Ensure Argo CD namespace
        run: |
          kubectl create namespace argocd --dry-run=client -o yaml | kubectl apply -f -

      - name: Install or upgrade Argo CD
        run: |
          kubectl apply --server-side -n argocd -f "https://raw.githubusercontent.com/argoproj/argo-cd/${ARGOCD_VERSION}/manifests/install.yaml"

      - name: Wait for Argo CD
        run: |
          kubectl wait --for=condition=available --timeout=300s deployment/argocd-server -n argocd

      - name: Argo CD UI (port-forward)
        run: |
          echo "kubectl port-forward svc/argocd-server -n argocd 8080:443"
          echo "kubectl -n argocd get secret argocd-initial-admin-secret -o jsonpath=\"{.data.password}\" | base64 -d; echo"

  bootstrap_apps:
    runs-on: ubuntu-latest
    needs: deploy_argocd
    environment: production
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure Alibaba Cloud credentials (OIDC)
        uses: aliyun/configure-aliyun-credentials-action@v1
        with:
          role-to-assume: ${{ secrets.ROLETOASSUME }}
          oidc-provider-arn: ${{ secrets.OIDC_PROVIDER }}
          audience: ${{ secrets.AUDIENCE }}

      - name: Set K8s context
        uses: aliyun/ack-set-context@v1
        with:
          access-key-id: ${{ env.ALIBABA_CLOUD_ACCESS_KEY_ID }}
          access-key-secret: ${{ env.ALIBABA_CLOUD_ACCESS_KEY_SECRET }}
          security-token: ${{ env.ALIBABA_CLOUD_SECURITY_TOKEN }}
          cluster-id: ${{ env.ACK_CLUSTER_ID }}

      - name: Apply app-of-apps (GitOps root)
        run: |
          kubectl apply -f bootstrap/root-app.yaml

  cleanup:
    runs-on: ubuntu-latest
    needs: deploy_argocd
    environment: cleanup
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure Alibaba Cloud credentials (OIDC)
        uses: aliyun/configure-aliyun-credentials-action@v1
        with:
          role-to-assume: ${{ secrets.ROLETOASSUME }}
          oidc-provider-arn: ${{ secrets.OIDC_PROVIDER }}
          audience: ${{ secrets.AUDIENCE }}

      - name: Set K8s context
        uses: aliyun/ack-set-context@v1
        with:
          access-key-id: ${{ env.ALIBABA_CLOUD_ACCESS_KEY_ID }}
          access-key-secret: ${{ env.ALIBABA_CLOUD_ACCESS_KEY_SECRET }}
          security-token: ${{ env.ALIBABA_CLOUD_SECURITY_TOKEN }}
          cluster-id: ${{ env.ACK_CLUSTER_ID }}

      - name: Remove app-of-apps
        run: |
          kubectl delete -f bootstrap/root-app.yaml --ignore-not-found

      - name: Remove Argo CD
        run: |
          kubectl delete -n argocd -f "https://raw.githubusercontent.com/argoproj/argo-cd/${ARGOCD_VERSION}/manifests/install.yaml"

      - name: Delete argocd namespace
        run: |
          kubectl delete namespace argocd
